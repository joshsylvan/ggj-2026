import type { BuzzerState } from "../types/buzz"

export const update = (deltaTime: number, buzzState: BuzzerState[]): void => { }

export const render = (deltaTime: number, canvas: HTMLCanvasElement, ctx: CanvasRenderingContext2D): void => { }